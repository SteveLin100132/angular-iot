import { DevicePanelDirective } from './device-panel.directive';

describe('DevicePanelDirective', () => {
  it('should create an instance', () => {
    const directive = new DevicePanelDirective();
    expect(directive).toBeTruthy();
  });
});
